# Optional server settings
PORT=3000
HOST=0.0.0.0

# Courtside currently uses server-side public data adapters and requires no API key.
# Never commit credentials if an authenticated provider is added later.
